请继续开发 v0.1，但不要扩展到完整 benchmark。

先做两个修复：

1. 修复 `bcm/retrieval/context_builder.py` 中 failed_commands 和 errors 的无序问题。必须按 `step_id` 升序排序，确保最早失败步骤优先。
2. 给 LOG 节点补边。优先新增 `EdgeType.HAS_LOG = "HAS_LOG"`，并在 `provenance_builder.py` 中创建 `COMMAND -> LOG` 边。同步更新 tests。

然后开始 v0.1：

实现 `bcm/ingest/bioagent_task_adapter.py`，用于从 `external/bioagent-bench/tasks/transcript-quant/` 抽取标准 task metadata。

实现 `bcm/ingest/trace_normalizer.py`，用于把 agent harness log 转成统一 trace JSON。v0.1 可以先支持手工 JSON passthrough 和简单 shell-step schema。

新增一个更真实的 fixture：

`data/runs/transcript_quant_bioagent_like_failed.json`

该 fixture 应包含多个 bioinformatics workflow steps，例如 index、quant、aggregate，并制造一个可解释失败，例如 wrong index path 或 missing quant.sf。

新增 tests：

- `tests/test_context_builder_ordering.py`
- `tests/test_log_edges.py`
- `tests/test_bioagent_task_adapter.py`
- `tests/test_realistic_transcript_quant_trace.py`

不要做：

- biological causal graph
- gene regulatory network
- expression matrix causal discovery
- LLM causal labeling
- full BioAgent-Bench execution
- database / Neo4j / web UI
- 修改 external/ 下的任何文件